class Solution {
    public boolean isPowerOfTwo(int n) {
        int i=0;
        boolean bool = false;
        while(Math.pow(2,i) <=n){
            if(Math.pow(2,i)==n){
                bool = true;
                break;
            }
            i++;
        }
        return bool;
    }
}
